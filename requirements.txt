# speech to text and text to speech
# Note: Using Python 3.12 for compatibility (realtimetts/TTS requires Python <3.13)
realtimestt==0.3.104
realtimetts[kokoro,coqui,orpheus]==0.5.5

# webserver dependencies
fastapi
uvicorn

# configuration
python-dotenv

# llm providers
ollama
openai

# testing dependencies (Phase 1)
pytest>=7.4.3
pytest-cov>=4.1.0
pytest-asyncio>=0.21.1
httpx>=0.25.0

# monitoring dependencies (Phase 1)
psutil>=5.9.6
